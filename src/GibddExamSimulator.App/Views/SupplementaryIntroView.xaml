<UserControl x:Class="GibddExamSimulator.Views.SupplementaryIntroView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
  <Grid Background="White">
    <Grid.RowDefinitions><RowDefinition Height="62" /><RowDefinition /><RowDefinition Height="72" /></Grid.RowDefinitions>
    <Border Background="#FFE8E8E8" BorderBrush="#FF777777" BorderThickness="0,0,0,1">
      <TextBlock Text="ДОПОЛНИТЕЛЬНЫЙ ЭТАП" FontWeight="Bold" FontSize="20" Margin="24,0" VerticalAlignment="Center" />
    </Border>
    <StackPanel Grid.Row="1" Width="760" VerticalAlignment="Center">
      <TextBlock Text="ИНФОРМАЦИЯ" FontSize="29" FontWeight="Bold" TextAlignment="Center" Margin="0,0,0,34" />
      <Border BorderBrush="#FF777777" BorderThickness="1" Background="#FFF5F5F5" Padding="32">
        <StackPanel>
          <TextBlock Text="{Binding SupplementaryTitle}" FontSize="20" FontWeight="Bold" TextWrapping="Wrap" Margin="0,0,0,20" />
          <TextBlock Text="{Binding SupplementaryDetails}" FontSize="18" TextWrapping="Wrap" Margin="0,0,0,12" />
          <TextBlock Text="Ошибки в дополнительном блоке не допускаются." FontSize="18" FontWeight="Bold" Foreground="#FFB00020" />
        </StackPanel>
      </Border>
      <Button Content="ПРОДОЛЖИТЬ" Command="{Binding StartSupplementaryCommand}"
              Width="240" Height="56" FontWeight="Bold" FontSize="19" Margin="0,30,0,0" />
    </StackPanel>
    <Border Grid.Row="2" Background="#FFF1F1F1" BorderBrush="#FFAAAAAA" BorderThickness="0,1,0,0">
      <TextBlock Text="Оставшееся время основного блока не переносится." FontSize="13"
                 HorizontalAlignment="Center" VerticalAlignment="Center" />
    </Border>
  </Grid>
</UserControl>
